# Claude Code Orchestration System - Python Dependencies
# Production requirements for deployment

# Core dependencies (no external dependencies required)
# Python 3.11+ standard library only:
# - json (stdlib)
# - asyncio (stdlib)
# - subprocess (stdlib)
# - pathlib (stdlib)
# - typing (stdlib)

# Development dependencies (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-timeout>=2.1.0

# Performance profiling (optional)
psutil>=5.9.0

# Code quality (optional)
black>=23.0.0
mypy>=1.5.0
ruff>=0.0.290
